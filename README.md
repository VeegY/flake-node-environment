**This setup requires the ``cross-env`` package as global npm package!**

To get submodule changes run:

``git pull`` and ``git submodule update --init``

or
``git submodule foreach git pull origin master``

